# 万界人生模拟器 —— 项目架构与状态诊断报告

> **生成日期**：2026-08-12
> **目标读者**：AI 架构师（深度分析与重构规划用）
> **分析方法**：全量代码扫描 + 依赖图分析 + 数据流追踪
> **状态**：客观诊断，不含修改建议

---

## 目录

1. [技术栈与依赖环境](#1-技术栈与依赖环境)
2. [核心目录与文件树](#2-核心目录与文件树)
3. [架构与数据流拆解](#3-架构与数据流拆解)
   - [3.1 状态管理](#31-状态管理)
   - [3.2 本地存储与数据持久化](#32-本地存储与数据持久化)
   - [3.3 API 交互层](#33-api-交互层)
   - [3.4 推演机制](#34-推演机制)
4. [代码坏味道与架构隐患](#4-代码坏味道与架构隐患)

---

## 1. 技术栈与依赖环境

### 前端

| 类别 | 技术 | 版本 | 备注 |
|------|------|------|------|
| **框架** | Vue 3 (Composition API / SFC) | ^3.5.0 | 纯 `<script setup>` 语法，无 TypeScript |
| **构建工具** | Vite | ^5.4.0 | 开发时 proxy `/api` → `127.0.0.1:8124`；生产时后端托管 `dist/` |
| **CSS** | Tailwind CSS | ^3.4.0 | 全局 `style.css` + Tailwind 原子类混用 |
| **Markdown** | markdown-it | ^14.1.0 | 用于回合叙述的 HTML 渲染（仅 break/linkify 选项） |
| **运行时** | Bun | — | `bun.lock` 锁文件，Node 亦可运行 |

**依赖极简**：前端仅 2 个运行时依赖（vue + markdown-it），无状态管理库（Pinia 等），无路由库（vue-router），无 UI 组件库。

### 后端

| 类别 | 技术 | 备注 |
|------|------|------|
| **HTTP 框架** | FastAPI | 异步但核心逻辑全程同步（`def` 而非 `async def`） |
| **服务器** | uvicorn | `uvicorn[standard]` |
| **AI SDK** | openai >=1.0 | 实际调用 DeepSeek API（base_url 指向 `api.deepseek.com`） |
| **数据校验** | pydantic | 请求体模型（NewGameRequest, ActRequest 等） |
| **环境变量** | python-dotenv | 仅 `.env.example`，无 `.env`（合法：key 可 BYOK） |
| **文档解析** | python-docx | 上传小说建世界时的 .docx 提取 |
| **测试** | pytest + httpx | 17 个测试文件 |

### AI 模型

- **模型**：`deepseek-chat`（硬编码为全局常量 `MODEL`）
- **温度**：叙述 0.85（流式）/ 0.7（统一 JSON），结算 0.3
- **输出格式**：`response_format: json_object`（统一回合调用）

---

## 2. 核心目录与文件树

```
万界人生模拟器/
├── frontend/                         # Vue 3 前端
│   ├── src/
│   │   ├── App.vue                   # ⭐ 根组件：视图路由 + 开局/读档/续玩全流程状态机
│   │   ├── store.js                  # ⭐ 全局状态仓库：game / ui / worlds / draft / entitlement
│   │   ├── api.js                    # ⭐ API 客户端：SSE 流消费 + REST 封装 + 超时控制
│   │   ├── md.js                     # Markdown 渲染 + 选项块剥离（前端副本）
│   │   ├── main.js                   # 入口：createApp 挂载
│   │   ├── views/
│   │   │   ├── HomeView.vue          # ⭐ 首页：世界广场 / 建世界 / 订阅状态 / 数据导入导出
│   │   │   └── GameView.vue          # ⭐ 游戏主视图：SSE 流消费 + 回合提交 + undo
│   │   └── components/
│   │       ├── CreationWizard.vue    # 魂兽大陆专属 5 步创建向导
│   │       ├── GenericWizard.vue     # 自建世界通用创建向导
│   │       ├── CharacterCard.vue     # 角色卡预览 → 确认开局
│   │       ├── NarrativeStream.vue   # 叙述流展示：流式 raw 文本 + 完成后 markdown 渲染
│   │       ├── OptionsBar.vue        # 选项栏（折叠胶囊）+ 自由输入 + "后悔"按钮
│   │       ├── StatusPanel.vue       # 状态抽屉：角色/资源/好感/魂环/笔记
│   │       ├── CharacterPanel.vue    # NPC 角色情报抽屉 + AI 提取 + 备注自动保存
│   │       ├── SavePanel.vue         # 存档/读档/导出面板
│   │       ├── FreedomControl.vue    # 自由度档位选择器（1-5）
│   │       └── ActivationPanel.vue   # 订阅/激活码弹窗
│   └── index.html                    # SPA 入口
│
├── backend/                          # Python 后端
│   ├── main.py                       # ⭐⭐⭐ God Object：API 入口 + SSE 生成器 + 会话管理 + 订阅门禁 + 世界系统
│   ├── game/
│   │   ├── __init__.py
│   │   ├── game_engine.py            # ⭐ 状态引擎：delta 校验 → 落账 → 好感衰减 → 回合推进
│   │   ├── state_schema.py           # ⭐ 权威状态模型：default_state / validate_delta / template_of
│   │   ├── prompt_builder.py         # ⭐ 提示词组装：规则书 + 状态摘要 + 历史 + 结构化 JSON 模板
│   │   ├── worlds.py                 # 世界规格：builtin/custom 加载/缓存/落盘/摘要
│   │   ├── world_builder.py          # 建世界流水线：小说抽样 → DeepSeek → 世界观 JSON 校验
│   │   ├── save_manager.py           # 会话持久化：state.json + history.jsonl + turns.json + undo.json
│   │   ├── novel_parser.py           # 小说解析：TXT 编码探测 + DOCX 提取 + 确定性抽样
│   │   ├── exporter.py               # 剧情导出：回合叙述 → Markdown 小说
│   │   └── activation_codes.py       # 激活码池：366 个随机码，每年循环
│   ├── worlds/
│   │   ├── douluo.json               # 魂兽大陆规格（builtin）
│   │   ├── gebi.json                 # 隔壁世界规格（builtin）
│   │   ├── taishangfuli.json         # 太上浮黎规格（builtin）
│   │   └── wanwusheng.json           # 万物生规格（builtin）
│   ├── prompts/
│   │   ├── system_prompt.txt         # 魂兽大陆规则书
│   │   ├── gebi_system_prompt.txt    # 隔壁规则书
│   │   ├── taishangfuli_system_prompt.txt
│   │   └── wanwusheng_system_prompt.txt
│   ├── data/
│   │   ├── activation_codes.json     # 码池（.gitignore 排除）
│   │   ├── worlds/                   # 自定义世界落盘目录
│   │   └── saves/                    # 存档目录
│   └── tests/                        # 17 个 pytest 文件
│
├── docs/
│   └── superpowers/
│       ├── plans/                    # 开发计划
│       └── specs/                    # 设计规格（多世界平台、魂兽大陆）
└── render.yaml                       # Render 平台部署
```

### 核心文件作用速览

| 文件 | 行数 | 一句话职责 |
|------|------|-----------|
| `backend/main.py` | 1138 | **God Object**：HTTP 路由 + SSE 流生成 + 会话缓存 + 订阅门禁 + 世界解析 + 导出/导入，全部在一个文件 |
| `frontend/src/store.js` | 177 | **全局状态仓库**：`game`/`ui`/`worlds`/`entitlement`/`draft` 五个 reactive 对象的单一模块 |
| `frontend/src/App.vue` | 159 | **前端编排器**：Home→Create→Review→Game 视图切换，开局与读档流程全在此 |
| `frontend/src/views/GameView.vue` | 143 | **游戏核心视图**：SSE 消费、回合提交、undo、`splitTurns` 逻辑 |
| `backend/game/game_engine.py` | 102 | **状态应用引擎**：资源/属性/好感增量 + 等级钳制 + 好感衰减 + 回合自增 |
| `backend/game/state_schema.py` | 272 | **权威状态模型**：`default_state`（初始状态生成）、`validate_delta`（AI 输出清洗与钳制） |
| `backend/game/prompt_builder.py` | 424 | **提示词工厂**：规则书加载 + 状态摘要（4 套世界专属格式硬编码）+ 历史消息裁剪 + 统一 JSON 模板 |

---

## 3. 架构与数据流拆解

### 3.1 状态管理

#### 前端状态（`store.js` — 单一全局 reactive 对象）

```
┌─────────────────────────────────────────────────────────┐
│  store.js: 5 个 reactive() 单例                          │
│                                                         │
│  game = {           ← 核心游戏状态（权威来自后端 delta）   │
│    sessionId, state, narrative, streaming, options,     │
│    notes, event, lastChoice, turns[], canUndo,         │
│    error, turnDone, turnCommitted                       │
│  }                                                     │
│                                                         │
│  ui = {             ← UI 状态（视图切换/面板开合）        │
│    view, showStatus, showSave, showCharacter, busy,    │
│    activationOpen, activationMsg                        │
│  }                                                     │
│                                                         │
│  worlds = {         ← 世界广场数据                       │
│    builtin[], mine[], selected, loading                 │
│  }                                                     │
│                                                         │
│  entitlement = {    ← 订阅/试玩状态                      │
│    paid, paidUntil, trialUsed, trialLimit,             │
│    subscriptionCode, loading                            │
│  }                                                     │
│                                                         │
│  draft = {          ← 创建向导草稿（20+ 字段）           │
│    identity, name, gender, age, origin, ...             │
│    apiKey, freedom   ← 同时缓存 localStorage             │
│  }                                                     │
└─────────────────────────────────────────────────────────┘
```

**流转方式**：

```
  后端 SSE (event: delta)
       │
       ▼
  GameView.vue / App.vue onDelta 回调
       │
       ├──→ setGameState(d.state)     ← 更新 game.state 权威数据
       ├──→ game.options = d.options   ← 直接赋值
       ├──→ game.canUndo = d.can_undo
       └──→ game.turnCommitted = true  ← 解锁归档
       
  组件间通信方式：
  - 所有组件直接 import { game, ui, worlds } from '../store'
  - 无 props 下传 / emit 上抛 的层次化数据流
  - 无 action / mutation 封装——组件直接对 reactive 对象赋值
```

**关键问题**：

- **无结构约束**：`game.state` 的结构随世界类型变化（`douluo` 有 `soul_level`/`soul_rings`，`gebi` 有 `heart`），前端通过 `isDouluo` computed 做分支判断，而不是通过 schema 驱动。
- **乐观更新与权威来源混用**：`CharacterPanel.vue` 中对 `customNotes` 先乐观改本地 `game.state.npcs`，再防抖落盘后端——若后端写入失败，前端无回滚机制。
- **同义函数重复**：`setGameState`（store.js）和 `setState`（GameView.vue）做完全一样的事——设定 `game.state` 和 `game.sessionId`。

#### 后端状态（`_SESSIONS` 内存字典）

```python
_SESSIONS: dict[str, dict] = {}   # 模块级全局变量，无 TTL

# 每个会话对象结构：
{
  "state": dict,          # 权威游戏状态
  "history": list[str],   # JSONL 行（user/assistant 消息）
  "archive": dict,        # 初始角色档案卡
  "last_options": list,   # 最新回合的选项
  "turns": list,          # 完整回合记录
  "undo_stack": list,     # 后悔快照栈（最多 20 层）
}
```

**关键问题**：

- **无 TTL / 淘汰**：会话创建后永驻内存，服务器运行越久内存越大。
- **无共享**：单进程内存存储——多 worker 部署时同一会话可能在不同 worker 上有不同版本。
- **冷启动恢复**：`_load_session()` 在 `_SESSIONS` 未命中时从磁盘加载——但磁盘与内存可能因 worker 不同步而不一致。

### 3.2 本地存储与数据持久化

#### 前端 localStorage（4 个 key）

| Key | 用途 | 读写位置 |
|-----|------|---------|
| `douluo_client_id` | 浏览器身份标识 | `store.js:clientId()` → 随每次 API 请求发送 |
| `douluo_api_key` | 玩家的 DeepSeek API Key | `store.js:saveApiKey()` / `draft.apiKey` |
| `douluo_freedom` | 自由度档位（1-5） | `store.js:setFreedom()` |
| `douluo_sub_code` | 激活码 | `store.js:setSubscriptionCode()` → 随核心请求发送 |

**安全隐患**：

- **API Key 明文存储**：`localStorage.setItem('douluo_api_key', k)` — 任何能访问该浏览器的 XSS 或恶意扩展都可读取。
- **激活码明文存储**：同上。激活码可被复制到其他设备使用（设计上允许跨设备，但无防盗用机制）。

#### 后端文件持久化

```
backend/data/
├── saves/
│   └── {session_id}/
│       ├── state.json          ← 完整游戏状态（每回合覆盖写）
│       ├── history.jsonl       ← 对话历史行（as user ➜ as assistant）
│       ├── turns.json          ← 回合记录（narrative + options + notes + event + choice）
│       ├── undo.json           ← 撤销栈快照
│       └── savepoints/
│           └── {session_id}-{timestamp}.json   ← 存档点
├── activations.json            ← 订阅登记表 {client_id: {paid_until, trial_used}}
└── worlds/
    └── {world_id}.json         ← 自定义世界规格
```

**原子性**：`save_activations` 用 `tmp → os.replace` 实现原子写；`save_state` 直接覆盖写（两文件分两步写，不原子——极端情况下 state.json 更新了但 history.jsonl 未更新）。

### 3.3 API 交互层

#### 请求链路全景

```
┌──────────────────────────────────────────────────────────┐
│                      前端 (Browser)                       │
│                                                          │
│  postSse(url, body, handlers)                            │
│    │                                                     │
│    ├─ POST /api/new-game                                 │
│    │   body: {world_id, archive, api_key, freedom,       │
│    │          client_id, code}                            │
│    │   → SSE: text/delta/error/done                      │
│    │                                                     │
│    ├─ POST /api/act                                      │
│    │   body: {session_id, action, api_key, freedom,      │
│    │          client_id, code}                            │
│    │   → SSE: text/delta/error/done                      │
│    │                                                     │
│    ├─ POST /api/undo       → JSON                        │
│    ├─ POST /api/resume     → JSON (不调 AI)              │
│    ├─ POST /api/save       → JSON                        │
│    ├─ POST /api/load       → JSON                        │
│    ├─ GET  /api/worlds     → JSON                        │
│    └─ ...（export/import/activate/extract-npc 等）        │
│                                                          │
│  后端调用 DeepSeek API:                                    │
│    client.chat.completions.create(                       │
│      model="deepseek-chat",                              │
│      response_format={"type": "json_object"},            │
│      ...                                                │
│    )                                                     │
└──────────────────────────────────────────────────────────┘
```

#### SSE 流协议

```
event: text
data: {"content": "一段叙述文本块..."}

event: text
data: {"content": "更多叙述..."}

event: delta
data: {"state": {...}, "options": [...], "notes": [...], "event": "...", "can_undo": true}

event: done
data: {}
```

#### API Key 路由

```
玩家请求携带 api_key?
  ├─ 是 → _client_for(api_key) → OpenAI(key=玩家key, base_url=deepseek)
  └─ 否 → _client_for(None)    → 服务端 _client
            ├─ 有 → 用服务端 key（DEEPSEEK_API_KEY 环境变量）
            └─ 无 → RuntimeError("NO_SERVER_KEY")
```

**解耦评估**：API 层（`api.js`）与业务逻辑相对独立——`postSse` 是通用 SSE 消费器，不感知游戏逻辑。但 **API Key 的存储和传递** 与业务代码深度耦合：`draft.apiKey` 从 `localStorage` 读取，随每次 `new-game`/`act` 请求"捎带"发送。

### 3.4 推演机制

#### 回合生命周期

```
[玩家选择 action]
    │
    ▼
GameView.submit(action)
    │
    ├─ commitTurn()          ← 归档上一回合（如果已完成）
    ├─ POST /api/act         ← 发送 action
    │
    ▼
后端 _run_turn()
    │
    ├─ build_unified_messages()  ← 构造完整提示词
    │   ├─ system_prompt(world)  ← 加载世界规则书
    │   ├─ state_summary()       ← 渲染当前状态为文本
    │   ├─ _history_messages()   ← 裁剪历史（双向限制：60条 + 20K token）
    │   └─ 自由度数要求            ← appended 到 user message
    │
    ├─ _call_turn()              ← 单次 JSON 调用 DeepSeek
    │   ├─ 重试1次（纠偏 nudge）
    │   └─ 仍失败 → 返回 {}
    │
    ├─ 模拟流式推送               ← 按中文标点边界切分，逐块 yield SSE
    │
    ├─ _normalize_options()      ← 选项补全（缺 label → ABCD；缺推荐 → 首个为推荐）
    │
    ├─ undo 快照保存             ← deepcopy(state + history + options)
    │
    ├─ apply_delta(state, delta) ← 状态变更落账
    │   ├─ validate_delta()      ← 钳制（资源 floor、等级 max_step、好感 range）
    │   ├─ 资源增减 + stats 增减 + 等级变更 + 好感更新 + 势力声望
    │   ├─ 好感衰减（按月推进、未见面角色）
    │   ├─ 魂环/道具/笔记 增删
    │   └─ location/date/season + turn++
    │
    ├─ turns 压入 + 持久化落盘   ← save_turns() + save_state()
    │
    ├─ _bump_trial()             ← 免费试玩计数
    │
    └─ yield delta SSE           ← {state, options, notes, event, can_undo}
    └─ yield done SSE
```

#### 关键设计

- **旧架构**：两阶段调用（自由文本叙述 → strip_options → ⚠️ 可能失败 → 兜底文本 → 结算 JSON）——已废弃。
- **新架构**：一次 `response_format=json_object` 调用同时产出 `narrative + options + state_delta + notes + event`。JSON 结构本身保证 narrative 和 options 物理隔离。
- **状态校验**：`validate_delta()` 是后端唯一信任 AI 产物的关口——对 AI 输出的每个字段做了类型强制、范围钳制、键名白名单过滤。
- **双上限历史裁剪**：`_history_messages()` 同时按条目数（60 条）和估算 token 数（20K）裁剪，从最新往旧逐条累加。

#### 耦合评估

| 维度 | 状态 | 说明 |
|------|------|------|
| 提示词构造 ↔ UI | **低耦合** | `prompt_builder.py` 完全不感知前端 |
| 状态应用 ↔ UI | **低耦合** | `game_engine.py` 纯函数式（就地修改 state dict） |
| 世界规格 ↔ 引擎 | **中耦合** | `validate_delta` 需传入 `world` 来决定钳制规则；4 套世界各有专属 `_xxx_summary()` 函数 **硬编码** 在 `prompt_builder.py` |
| 回合流 ↔ HTTP | **高耦合** | `_run_turn()` 是 SSE 生成器，与 FastAPI `StreamingResponse` 紧绑定——无法脱离 HTTP 上下文独立调用 |
| 会话管理 ↔ 业务逻辑 | **高耦合** | `_SESSIONS` 全局 dict 被多个路由函数直接读写；load/save 调用散落在路由处理逻辑中 |

---

## 4. 代码坏味道与架构隐患

### 🔴 P0 — 架构级：God Object `main.py`

**文件**：[`backend/main.py`](backend/main.py) — **1138 行**

该文件承载了以下全部职责，任何一项变更都需要触碰这个文件：

| 职责 | 行数范围 | 说明 |
|------|---------|------|
| 环境初始化与全局变量 | L1-L64 | 环境变量、OpenAI client、模型常量、token 上限、自由度档位 |
| 订阅门禁逻辑 | L67-L224 | 码池校验、中国时区墙钟、paid_status、gate_for、bump_trial |
| 世界系统路由逻辑 | L227-L253 | 世界解析、自建限流、构建历史 |
| 会话内存管理 | L256-L277 | `_SESSIONS` dict、`_new_session`、`_load_session` |
| 请求模型定义 | L280-L343 | 10+ 个 pydantic BaseModel |
| SSE 工具函数 | L363-L529 | `_sse`、`_stream_narrative`（旧架构）、`_call_settle`（旧）、`_call_turn`、`_strip_options_block`、`_normalize_options` |
| **回合生成核心** | **L535-L662** | `_run_turn()` — SSE 生成器，包含完整回合流水线 |
| API 路由 | L665-L1137 | 18 个端点（health/saves/new-game/act/activate/entitlement/resume/undo/save/load/delete/update-npcs/extract-npc-profiles/export-all/import-all/export/worlds/build/delete） |

**后果**：任何新功能（如新端点、新的订阅策略、新的生成优化）都会进一步膨胀此文件。测试困难——`_run_turn()` 作为嵌套闭包生成器，无法独立注入 mock。

---

### 🔴 P1 — 状态管理：全局可变单例 + 无约束

**前端**（[`store.js`](frontend/src/store.js)）：

- **5 个 `reactive()` 导出对象** 作为全局状态——任何组件都可以 `import { game } from '../store'` 并直接 `game.narrative = ''`，无从追踪"谁在什么时候改了什么"。
- **`game` 对象承担 15 个属性**，其中 `state`（权威状态）、`turns`（归档回合）、`narrative`（流式文本）、`streaming`/`turnDone`/`turnCommitted`（流程控制旗标）、`error`（错误信息）全部平铺——结构语义丢失。
- **`draft` 对象** 混合了**临时草稿数据**和**持久化配置**（`apiKey`、`freedom`）——后者应属于用户配置模块，而非创建向导草稿。

**后端**（[`main.py:L257`](backend/main.py#L257)）：

- `_SESSIONS: dict[str, dict] = {}` — 模块级全局可变字典，无 TTL、无淘汰策略、无容量上限。
- 会话对象是一个 **无类型的裸 dict**（`{"state": ..., "history": ..., ...}`），通过字符串键访问——无法利用 IDE 自动补全或静态分析。

**后果**：

- 多 worker 部署时会话数据不一致。
- 服务器长时间运行导致内存泄漏（永不过期的会话）。
- 前端调试困难——无法回答"`game.narrative` 在第 N 回合后为什么会变成空串？"

---

### 🟡 P2 — 耦合：世界特定逻辑硬编码

**文件**：[`prompt_builder.py`](backend/game/prompt_builder.py) — L71-L206

4 套世界各有专属的 `state_summary` 渲染函数：

```python
_douluo_summary(state)       # L141-L162 — 魂兽大陆
_gebi_summary(state)         # L122-L138 — 隔壁
_taishangfuli_summary(state)  # L77-L97  — 太上浮黎
_wanwusheng_summary(state)    # L100-L119 — 万物生
```

以及 `state_summary()` 中的 if-else 分发链（L194-L206）：

```python
if s == "douluo":       return _douluo_summary(state)
if s == "wanwusheng":   return _wanwusheng_summary(state)
if s == "gebi":         return _gebi_summary(state)
if s == "taishangfuli": return _taishangfuli_summary(state)
return _generic_summary(state, world)
```

**设计矛盾**：项目已设计了通用 `state_template` 和 `_generic_summary()` 来驱动自建世界——但内置世界不走通用路径，而是硬编码了专属格式化函数。**每新增一个内置世界，就需要改 `prompt_builder.py`**。

在 [`StatusPanel.vue`](frontend/src/components/StatusPanel.vue) 前端也存在同样问题：

```javascript
const isDouluo = computed(() => !st.value?.meta?.world_id || st.value.meta.world_id === 'douluo')
```

模板中 `v-if="isDouluo"` / `v-else` 大量分支——魂兽大陆的 UI 结构被硬编码进了组件。

---

### 🟡 P2 — 逻辑重复：前后端镜像实现

| 功能 | 前端实现 | 后端实现 |
|------|---------|---------|
| 选项块剥离 | [`md.js:stripOptionsBlock()`](frontend/src/md.js) | [`main.py:_strip_options_block()`](backend/main.py#L469-L491) |
| 叙述判空阈值 | 无（依赖后端 error 事件） | `NARRATIVE_MIN_CHARS = 30` |
| API Key 存储 | `localStorage.getItem('douluo_api_key')` 在 3 个组件中独立读取 | `req.api_key` 从请求体获取 |

两端对"什么是有效的叙述正文"的判定逻辑不完全一致——前端通过 `stripOptionsBlock` 处理，后端通过 `_strip_options_block` + `NARRATIVE_MIN_CHARS` 双重判定。

---

### 🟡 P2 — 错误处理：静默吞异常

在全量扫描中发现大量如下模式：

```python
# 后端
except Exception:
    pass   # L644: 试玩计次失败 → 吞掉
    pass   # L651: 自动存档失败 → 吞掉

# 前端
} catch { /* 后端未启动等场景静默 */ } finally { ... }
} catch { /* 静默失败；下次保存时自动重试 */ }
```

这些静默吞异常的设计意图是"某一模块失败不应拖垮全局"，但代价是：

- **自动存档失败无反馈**：玩家第 91 回合满心期待，重启后发现只能从第 80 回合的存档点恢复。
- **试玩计数失败**：玩家可能无限免费游玩（计数器写失败时后端不报错）。
- **乐观更新不一致**：`CharacterPanel.vue` 中对 NPC 备注先更新本地 state，后异步落盘——如果落盘失败，**无回滚无重试无提示**。

---

### 🟡 P3 — 时间处理：多时区混用

**文件**：[`main.py`](backend/main.py)

- 订阅门禁使用**中国时区（UTC+8）**：`_cn_now()`、`_CN_TZ`
- 导出时间戳使用 **UTC**：`datetime.now(timezone.utc).isoformat()`
- 前端展示使用**浏览器本地时区**：`new Date(iso).toLocaleDateString('zh-CN', ...)`

三种时区混用，加上"码池日期"和"订阅到期日"的跨年边界处理（L157-L167），这段逻辑在多处被复制——任何时区 bug 的表现都不是报错而是一声不吭的"码已过期"或"订阅还在却不让玩"。

---

### 🟡 P3 — 无结构化日志

整个项目无日志框架。所有错误通过两种方式暴露：

- 前端：`alert(e.message)` — 弹窗，无法回溯
- 后端：`raise HTTPException(status, detail)` — 返回给前端，服务器无记录

**后果**：生产环境下，一个玩家报告"第三回合之后就没反应了"，开发者无法通过日志定位是 DeepSeek 超时、JSON 解析失败、还是磁盘写入错误。

---

### 🟢 P4 — 缺少输入校验层

- 前端 `freeInput`（玩家自由输入）直接作为 `action` 发给后端，不经清洗——如果玩家输入包含对 AI 的越狱指令（如 `忽略之前所有规则`），后端的唯一防御是 DeepSeek 自身的内容安全策略。
- `extractNpcProfiles` 的 `npc_names` 列表直接插入 prompt，未做转义或长度限制。

---

### 🟢 P4 — 测试覆盖

17 个测试文件覆盖了核心逻辑（game_engine、state_schema、prompt_builder、worlds、undo、exporter 等），但：

- **无端到端测试**：没有对 `/api/act` SSE 流的完整集成测试。
- **无前端测试**：Vue 组件零测试覆盖。
- **`_run_turn()` 未被直接测试**——作为最复杂的核心流水线，它只能通过端到端手动测试验证。

---

### 📊 架构隐患总结矩阵

| 隐患 | 严重度 | 影响范围 | 当前是否引发可观测 Bug |
|------|--------|---------|----------------------|
| `main.py` God Object | 🔴 高 | 所有后端变更 | 否（技术债累积） |
| `_SESSIONS` 无 TTL → 内存泄漏 | 🔴 高 | 长期运行的服务器 | 是（长时间运行后内存增长） |
| 前端全局可变状态无约束 | 🟡 中 | 所有前端组件 | 是（状态不同步时的 UI 闪烁/跳动） |
| 世界特定逻辑硬编码 | 🟡 中 | 新增内置世界 | 否（当前仅 4 个世界） |
| 前后端选项剥离逻辑重复 | 🟡 中 | 叙述展示 | 是（偶现叙述末尾残留选项字符） |
| 静默吞异常 | 🟡 中 | 存档/订阅计数 | 是（自动存档失败无提示） |
| 时区混用 | 🟡 中 | 订阅激活 | 潜在（未报告但存在边界条件） |
| 无结构化日志 | 🟢 低 | 调试/运维 | 是（线上问题无服务端日志可查） |
| API Key 明文 localStorage | 🟢 低 | 安全性 | 否（设计权衡） |
| SSE 生成器与 HTTP 紧耦合 | 🟡 中 | 架构可测试性 | 否（技术债） |

---

## 附录 A：数据流全局图

```
┌──────────────────────────────────────────────────────────────────┐
│                         浏览器                                    │
│                                                                  │
│  localStorage          store.js (reactive)         Vue 组件       │
│  ┌──────────┐         ┌──────────────┐         ┌──────────────┐  │
│  │api_key   │────────▶│ draft.apiKey │────────▶│ CreationWizard│  │
│  │client_id │────────▶│ clientId()   │────────▶│ GameView      │  │
│  │freedom   │────────▶│ draft.freedom│────────▶│ HomeView      │  │
│  │sub_code  │────────▶│ entitlement  │────────▶│ ActivationPanel│ │
│  └──────────┘         │ game.state ◀─│────────│ StatusPanel   │  │
│                       │ game.turns   │        │ CharacterPanel│  │
│                       └──────┬───────┘        └──────────────┘  │
│                              │                                    │
│                    api.js    │                                    │
│                    ┌─────────▼──────────┐                        │
│                    │ SSE onDelta →       │                       │
│                    │ setGameState(d.state)│                      │
│                    │ game.options = ...   │                       │
│                    └─────────┬──────────┘                        │
└──────────────────────────────┼───────────────────────────────────┘
                               │ HTTP POST + SSE (text/delta/done)
                               ▼
┌──────────────────────────────────────────────────────────────────┐
│                        后端 (FastAPI)                             │
│                                                                  │
│  main.py                                                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ _run_turn() — SSE Generator                                 │  │
│  │   ├─ build_unified_messages()                               │  │
│  │   │    ├─ system_prompt(world)  ← prompt_builder.py         │  │
│  │   │    ├─ state_summary(state)  ← 4套硬编码 or _generic     │  │
│  │   │    └─ _history_messages()   ← 双上限裁剪                │  │
│  │   ├─ _call_turn() → DeepSeek API                            │  │
│  │   ├─ _normalize_options()                                   │  │
│  │   ├─ apply_delta(state, delta) ← game_engine.py             │  │
│  │   │    └─ validate_delta() ← state_schema.py                │  │
│  │   ├─ save_state() + save_turns() ← save_manager.py          │  │
│  │   └─ yield SSE events                                       │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  _SESSIONS: dict (内存)                                          │
│  data/saves/{sid}/state.json (磁盘)                              │
│  data/worlds/{wid}.json (磁盘)                                   │
│  data/activations.json (磁盘)                                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 附录 B：已识别的命名不一致

| 不一致 | 位置 | 说明 |
|--------|------|------|
| `soul_level` vs `level_field` | `state_schema.py` / `worlds/*.json` | 魂兽大陆硬编码 `soul_level`，通用世界用 `level_field` 抽象，但多处仍假设存在 `soul_level` |
| `douluo` vs `魂兽大陆` | 全仓 | 内部 id 用 `douluo`，展示用 `魂兽大陆`，`isDouluo` 分支散落各处 |
| `archive` vs `character` | 前端 `draft` / 后端 `archive` | 前端草稿叫 `draft`，提交时字段展平为 `archive.character`，后端又解包回 `ch` |
| `session_id` vs `sessionId` | 前端 `game.sessionId` / 后端 `session_id` | Python 用 snake_case，JS 用 camelCase——字段传递时通过 JSON key 自动转换，但前端 store 里混用了 `sessionId` 和直接读 `state.meta.session_id` |

---

> **报告结束。**
> 
> 本报告基于对 `107` 个源文件的全面扫描生成。所有行号引用基于 2026-08-12 的代码快照。
